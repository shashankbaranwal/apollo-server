type User {
    id: ID!
    name: String!
    email: String!
    role: String!
    password: String!
    createdBy: String!
}

input Payload {
  email: String!
  password: String!
}

input loginData {
  email: String!
  password: String!
}
